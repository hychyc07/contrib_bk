cmake_minimum_required(VERSION 2.6)

FIND_PACKAGE(ICUB)

MESSAGE(STATUS ${ICUB_INCLUDE_DIRS})
INCLUDE_DIRECTORIES(${ICUB_INCLUDE_DIRS})
ADD_EXECUTABLE(example1 main.cpp)
TARGET_LINK_LIBRARIES(example1 sharksWithLasers)
