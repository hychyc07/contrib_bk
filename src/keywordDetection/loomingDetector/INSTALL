Installation instructions:

Dependencies:
 * Yarp: http://wiki.icub.org/yarp/
 * iCub: https://sourceforge.net/projects/icub/

Installation:
 * Go to the root directory of the project (where this file is located)
 * Create a build directory
	mkdir build
 * Change to that directory
	cd build
 * Run cmake
	cmake ..
   or
	cmake .. -DCMAKE_INSTALL_PREFIX=<prefix>
 * Run make
	make
 * Run make install
	make install 
